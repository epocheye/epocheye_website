export const metadata = {
  title: "Delete Your Account — Epocheye",
  description:
    "Request deletion of your Epocheye account and associated data.",
};

export default function DeleteAccountLayout({ children }) {
  return children;
}

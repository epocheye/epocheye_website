# Supabase (reuse existing project)
SUPABASE_URL=https://altbhqoiiwgpyabecwom.supabase.co
SUPABASE_SERVICE_KEY=your_supabase_service_key_here

# JWT secrets — generate with: openssl rand -base64 32
CREATOR_JWT_SECRET=your_creator_jwt_secret_here
CREATOR_JWT_REFRESH_SECRET=your_creator_refresh_jwt_secret_here

# Razorpay (for automated UPI payouts)
RAZORPAY_KEY_ID=rzp_live_xxxxxxxxxxxx
RAZORPAY_KEY_SECRET=your_razorpay_key_secret

# Webhook secret — shared with mobile app backend for conversion events
# Generate with: openssl rand -hex 32
WEBHOOK_SECRET=your_webhook_secret_here

# Admin API key — for admin dashboard access
# Generate with: openssl rand -hex 32
ADMIN_API_KEY=your_admin_api_key_here

# CORS
CORS_ORIGIN=https://epocheye.app

# Local dev only
PORT=3001
